# Yandex Cloud VM Deployment

This repository automates the deployment of a **Yandex Cloud** Virtual Machine using **Terraform** and configures it with **Ansible**:

- The pipeline creates the infrastructure and sets up an **Nginx** web server on an Ubuntu VM.
> **Note:** This is just for test purposes 
